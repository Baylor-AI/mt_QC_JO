git status
git commit -m "Message" -m "detail message"

Adding local folder to git
1. Init git
    git Init
2. Git status 
    git status
    Add it to the file.
3. We get error while pushing to github.
    git remote add origin githubrepo link
4. Git remote -v
5. Upstream
    git push -u origin master


6. Git Branching
    6.1 git Branch
    Creating a new Branch
    git checkout -b "name of Branch"
7. Swtich between Branch
    git checkout "branchname"

8. Check difference between two Branch
9. git merge
10. Didn't work on git push 
    git push --set-upstream origin new-branch

11. Delete Branch
    git branch -d name






